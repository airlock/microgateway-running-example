apiVersion: apps/v1
kind: Deployment
metadata:
  name: grafana
  namespace: monitoring
spec:
  template:
    spec:
      containers:
      - name: grafana
        env:
        - name: GRAFANA_DATASOURCE_TOKEN
          valueFrom:
            secretKeyRef:
              name: grafana-datasource-token
              key: token
