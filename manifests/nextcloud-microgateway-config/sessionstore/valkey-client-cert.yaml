---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: valkey-client-cert
  namespace: nextcloud
spec:
  dnsNames:
  - airlock.nextcloud.svc.cluster.local
  issuerRef:
    kind: ClusterIssuer
    name: cluster-ca-issuer
  secretName: valkey-client-cert
  usages:
  - client auth
  privateKey:
    size: 4096
  commonName: "CN=airlock.nextcloud.svc.cluster.local"
