---
apiVersion: v1
kind: Secret
metadata:
  name: valkey-password
  namespace: airlock-gateway
stringData:
  # RedisProvider requires this key for Redis-compatible stores such as Valkey.
  redis.password: "runningexample"
