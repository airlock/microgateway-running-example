---
operator:
  gatewayAPI:
    enabled: true
  serviceMonitor:
    create: true
    labels:
      release: "kube-prometheus-stack"
engine:
  sidecar:
    podMonitor:
      create: true
      labels:
        release: "kube-prometheus-stack"
dashboards:
  create: true
